<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArmaReforgerServerToolWinUI.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArmaReforgerServerToolWinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SolidBackgroundFillColorTransparent}">

    <Grid>
        <StackPanel
        CornerRadius="8"
        Padding="12"
        BorderThickness="1"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
        <InfoBar
        IsOpen="True"
        Severity="Success"
        Title="You're up to date!"
        Message="You are currently using the latest version :)" />
            <TextBlock Padding="12"  FontSize="24" Text="What's new?"/>
            <StackPanel
                CornerRadius="8"
                Padding="12"
                BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <TextBlock Padding="12"  FontSize="14" Text="Github changelog will go here"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
